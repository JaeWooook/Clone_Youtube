extends base

block content 
    form(method="POST", action="/videos/upload")
        input(name="newTitle",placeholder="Title", required, type="text")
        input(type="submit", value="Upload Video")